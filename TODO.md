# TODO

- [ ] Inspect existing CNN-related lesson files and routing/registry for where to place a CNN simulation page.
- [ ] Implement a “CNN explainer–style” interactive convolution/pooling visualization (forward pass) using existing simulation UI patterns (e.g., GraphSimulation / other SimulationLab components).
- [ ] Add a new lesson file (or update an existing placeholder) under `content/artificial-intelligence/03-deep-learning/` for the CNN simulation.
- [ ] Update any content registry / lesson loader mapping if needed so the new lesson appears in the app.
- [ ] Run lint/build (if configured) to ensure TypeScript/Next.js compilation passes.

